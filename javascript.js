//this will be the function that checks if the "ball" hits the wall. If it does then we will have the ball change directions.
function bounce(position){
  var yVel = 10;//Both values wil lstart at a default speed of 10 and they will either always be positive or negitve 10
  var yVel = 10;

  if(position.x = 0)
}
